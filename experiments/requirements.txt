# GPU 实验 Python 依赖（stage-1；计费口径已改模板工作单元，见 docs/stage1/decision-log.md D1，
# 本目录实验保留为执行真实性研究）
#
# torch 使用 CUDA 版 Linux 轮子（Linux x86_64 的 PyPI 官方 wheel 自带 CUDA 运行时）。
# 安装请优先运行 setup_wsl.sh（uv 项目模式，默认走清华 PyPI 镜像）：
#
#   ./setup_wsl.sh
#
# 手动安装（等效）：
#   export UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple
#   uv venv --python 3.12
#   uv add "torch>=2.4" "pynvml>=11.5"
#
# ncu（Nsight Compute CLI）不是 pip 包（PyPI 无 nvidia-nsight-compute，实测 404）：
# 用 NVIDIA redist archive 用户级安装（免 sudo，实测可用）：
#   curl -sL -o /tmp/ncu.tar.xz "https://developer.download.nvidia.com/compute/cuda/redist/nsight_compute/linux-x86_64/nsight_compute-linux-x86_64-2025.1.1.2-archive.tar.xz"
#   mkdir -p ~/opt && tar -xf /tmp/ncu.tar.xz -C ~/opt && ln -sfn ~/opt/nsight_compute-*-archive ~/opt/nsight-compute
#   export PATH="$HOME/opt/nsight-compute:$PATH"
# 注意：WSL2 与 GPU 容器内实测报 ERR_NVGPUCTRPERM（宿主内核参数，容器/WSL 内无法放行），
# 仅原生 Linux（root）可采硬件计数——见 docs/stage1/gpu-metering-report.md §8。

torch>=2.4
pynvml>=11.5
